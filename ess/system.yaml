format: ess/1
system: extensions
version: v1
summary: >-
  Draft causal control-domain model for independently released extensions and named tenant
  installations. Binding and activation contracts are declared; runtime execution, authorization
  and persistence remain unimplemented.
domains:
  - extensions.control
